% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{autoplot.ref_fit}
\alias{autoplot.ref_fit}
\alias{autoplot.ref_assessment}
\alias{autoplot.ref_scores}
\alias{autoplot.ref_forecast}
\alias{autoplot.ref_derivative}
\alias{autoplot.ref_transition}
\alias{autoplot.ref_dynamics}
\title{Graphics for reference models}
\usage{
\method{autoplot}{ref_fit}(
  object,
  type = c("centiles", "trajectories", "support", "adaptation"),
  outcome = NULL,
  x = NULL,
  newdata = NULL,
  by = NULL,
  data = NULL,
  id = NULL,
  time = NULL,
  centiles = c(0.05, 0.25, 0.5, 0.75, 0.95),
  ...
)

\method{autoplot}{ref_assessment}(
  object,
  type = c("calibration", "worm", "qq", "conditional"),
  level = 0.95,
  ...
)

\method{autoplot}{ref_scores}(object, type = c("profile", "heatmap"), id = NULL, ...)

\method{autoplot}{ref_forecast}(
  object,
  type = c("fan"),
  centiles = c(0.05, 0.25, 0.5, 0.75, 0.95),
  ...
)

\method{autoplot}{ref_derivative}(object, type = c("velocity"), level = 0.95, ...)

\method{autoplot}{ref_transition}(object, type = c("velocity", "innovation", "change"), ...)

\method{autoplot}{ref_dynamics}(
  object,
  type = c("kernel", "calibration", "thrive"),
  data = NULL,
  id = NULL,
  time = NULL,
  level = NULL,
  outcome = NULL,
  x = NULL,
  centiles = c(0.05, 0.25, 0.5, 0.75, 0.95),
  anchors = c(0.05, 0.25, 0.5, 0.75, 0.95),
  from = NULL,
  horizon = 1,
  thrive = 0.025,
  ...
)
}
\arguments{
\item{object}{A fit, assessment, score table, forecast, transition,
or dynamics object.}

\item{type}{Plot kind.}

\item{outcome}{Outcome name for multi-outcome fits.}

\item{x}{Covariate mapped to the x-axis (default \code{age} if present).}

\item{newdata}{Observations to overlay or to classify.}

\item{by}{Optional grouping factor for faceted charts.}

\item{data}{Visit-level data for trajectory and dynamics-calibration
plots.}

\item{id}{Subject identifier column.}

\item{time}{Time column.}

\item{centiles}{Probability levels for centile and fan charts.}

\item{...}{Unused.}

\item{level}{Coverage of the envelope on the Q-Q and worm plots, of
the pointwise interval on a velocity plot, or of the correlation
interval around a thrive line.}

\item{anchors}{Starting centiles for \code{type = "thrive"}.}

\item{from}{Anchor times for \code{type = "thrive"} (default: a grid over
the reference range).}

\item{horizon}{Forward step(s) for \code{type = "thrive"}; a vector traces a
continuous path rather than a single segment.}

\item{thrive}{Conditional centile of the step for \code{type = "thrive"};
\code{0.5} gives the pure regression-to-the-mean path.}
}
\value{
A ggplot.
}
\description{
Every \code{autoplot()} method returns a \code{ggplot} object styled with
\code{\link[=theme_referent]{theme_referent()}}. Individual observations are overlays, not part of
the fitted geometry.
}
\details{
For a \link{ref_fit}:
\itemize{
\item \code{"centiles"}: the fitted conditional distribution along \code{x}
(median, dashed outer centiles, and shaded bands), optionally faceted
by a factor covariate and with \code{newdata} overlaid as points.
\item \code{"trajectories"}: the centile chart with subject paths from \code{data}
drawn over it.
\item \code{"support"}: where \code{newdata} falls relative to the reference support
(histogram of \code{x} coloured by \code{\link[=ref_support]{ref_support()}} status).
\item \code{"adaptation"}: location offsets (with standard errors) estimated by
\code{\link[=ref_adapt]{ref_adapt()}}.
}

For a \code{ref_assessment}, \code{"calibration"} plots observed against nominal
coverage, \code{"conditional"} the largest fitted drift of \code{z} against each
numeric covariate (factor levels are in the table only), and \code{"qq"} / \code{"worm"} the ordered Z scores against their
expected normal order statistics, raw or detrended (observed minus
expected), inside the envelope a calibrated model implies. Under a
correctly specified model the held-out centiles are iid uniform, so
the i-th ordered centile is Beta(i, n - i + 1): the pointwise band is
its normal transform in closed form, and the simultaneous band uses
equal local levels (Aldor-Noiman et al. 2013, Am. Stat. 67:249) with
the common local level calibrated by Monte Carlo so the whole ordered
sample stays inside with probability \code{level}. The corner tally names
both bands, since about \code{1 - level} of the points are expected outside
the pointwise band even when the model is right.

For a \code{ref_dynamics}, \code{"kernel"} draws the fitted process correlation
against lag, \code{"calibration"} draws the same Q-Q chart of held-out
innovation Z from \code{\link[=ref_transition]{ref_transition()}} on \code{data} (which needs \code{data},
\code{id}, and \code{time}), and \code{"thrive"} overlays conditional forecast
quantile paths on the centile chart. A thrive line starts on the
\code{anchors} centile at each time in \code{from} and follows the \code{thrive}
quantile of the forecast distribution \code{horizon} time units later; it is
\code{\link[=ref_forecast]{ref_forecast()}} on a grid of one-visit histories, so the shaded band
is the \code{level} interval implied by the standard error of the estimated
process correlation. Paths whose lag falls outside the range the
dynamics were estimated on are drawn dotted.

For a \link{ref_derivative}, \code{"velocity"} draws the chart velocity of each
centile against time with its pointwise delta-method interval. That
interval is the sampling error of the fitted coefficients; it does not
cover the bias of the spline itself, which dominates when the basis
dimension \code{k} of the time smooth is too small. The subtitle says so.
}
\examples{
ref <- ref_simulate(150, seed = 1)
fit <- ref_fit(ref_spec(ref_gaussian(), ~ s(age, k = 5) + sex), ref, "y")
autoplot(fit, type = "centiles", by = sex, newdata = ref[1:20, ])
sc <- predict(fit, newdata = ref[1:6, ], uncertainty = "conditional")
autoplot(sc, type = "heatmap")
}
