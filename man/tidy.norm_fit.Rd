% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidiers.R
\name{tidy.norm_fit}
\alias{tidy.norm_fit}
\alias{glance.norm_fit}
\alias{glance.norm_assessment}
\alias{augment.norm_fit}
\title{Tidy summaries of reference fits and assessments}
\usage{
\method{tidy}{norm_fit}(x, ...)

\method{glance}{norm_fit}(x, ...)

\method{glance}{norm_assessment}(x, ...)

\method{augment}{norm_fit}(x, newdata, uncertainty = c("total", "conditional"), ...)
}
\arguments{
\item{x}{A \link{norm_fit} or \code{norm_assessment}.}

\item{...}{Passed to \code{\link[=predict.norm_fit]{predict.norm_fit()}} by \code{augment()}; unused
otherwise.}

\item{newdata}{Data frame of target observations.}

\item{uncertainty}{Passed to \code{\link[=predict.norm_fit]{predict.norm_fit()}}.}
}
\value{
A tibble.
}
\description{
\code{tidy()} gives one row per outcome: family, fit status, sample size,
total effective degrees of freedom and one \verb{edf_<term>} column per
smooth term, and any engine message. \code{glance()} gives one row per fit
(or per assessment, averaging the \code{overall} and \code{marginal} tables over
outcomes). \code{augment()} returns \code{newdata} with \verb{.z_<outcome>} and
\verb{.centile_<outcome>} columns and the covariate \code{.support}; the result
can be passed straight to \code{\link[=norm_joint]{norm_joint()}}.
}
\examples{
ref <- norm_simulate(120, seed = 3)
fit <- norm_fit(norm_spec(norm_gaussian(), ~ s(age, k = 5) + sex), ref, "y")
tidy(fit)
glance(fit)
augment(fit, ref[1:3, ], uncertainty = "conditional")
}
