% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidiers.R
\name{tidy.ref_fit}
\alias{tidy.ref_fit}
\alias{glance.ref_fit}
\alias{glance.ref_assessment}
\alias{augment.ref_fit}
\title{Tidy summaries of reference fits and assessments}
\usage{
\method{tidy}{ref_fit}(x, ...)

\method{glance}{ref_fit}(x, ...)

\method{glance}{ref_assessment}(x, ...)

\method{augment}{ref_fit}(x, newdata, uncertainty = c("total", "conditional"), ...)
}
\arguments{
\item{x}{A \link{ref_fit} or \code{ref_assessment}.}

\item{...}{Passed to \code{\link[=predict.ref_fit]{predict.ref_fit()}} by \code{augment()}; unused
otherwise.}

\item{newdata}{Data frame of target observations.}

\item{uncertainty}{Passed to \code{\link[=predict.ref_fit]{predict.ref_fit()}}.}
}
\value{
A tibble.
}
\description{
\code{tidy()} gives one row per outcome: family, fit status, sample size,
total effective degrees of freedom and one \verb{edf_<term>} column per
smooth term, and any engine message. \code{glance()} gives one row per fit
(or per assessment, averaging the \code{overall} and \code{marginal} tables over
outcomes). \code{augment()} returns \code{newdata} with \verb{.z_<outcome>} and
\verb{.centile_<outcome>} columns and the covariate \code{.support}; the result
can be passed straight to \code{\link[=ref_joint]{ref_joint()}}.
}
\examples{
ref <- ref_simulate(120, seed = 3)
fit <- ref_fit(ref_spec(ref_gaussian(), ~ s(age, k = 5) + sex), ref, "y")
tidy(fit)
glance(fit)
augment(fit, ref[1:3, ], uncertainty = "conditional")
}
