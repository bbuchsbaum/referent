% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dist.R
\name{norm_dist}
\alias{norm_dist}
\title{Predictive distribution objects}
\usage{
norm_dist(
  family,
  location,
  scale,
  skew = NULL,
  tail = NULL,
  aleatoric_sd = NULL,
  epistemic_sd = NULL
)
}
\arguments{
\item{family}{Family name or a \link{norm_family} object.}

\item{location, scale, skew, tail}{Numeric parameter vectors. Recycled to
a common length.}

\item{aleatoric_sd, epistemic_sd}{Optional uncertainty summaries,
recycled to the same length.}
}
\value{
An object of class \code{norm_dist}.
}
\description{
A vectorized conditional predictive distribution. Every engine must
return an object that supports this contract: \code{cdf()}, \code{quantile()},
\code{log_density()}, \code{draw()}, \code{center()}, and \code{variance()}.
}
\details{
Parameters follow the \code{mgcv} SHASH / Gaussian location-scale
conventions:
\itemize{
\item \code{location} (\eqn{\mu})
\item \code{scale} (\eqn{\sigma > 0})
\item \code{skew} (\eqn{\epsilon}; SHASH only)
\item \code{tail} (\eqn{\delta > 0}; SHASH only)
}

For SHASH, \eqn{z = (y-\mu)/(\sigma\delta)} and
\eqn{F(y) = \Phi(\sinh(\delta\,\mathrm{asinh}(z)-\epsilon))}.
}
\examples{
d <- norm_dist("gaussian", location = 0, scale = 1)
cdf(d, 1.96)
as_scores(d, c(-2, 0, 2))
}
