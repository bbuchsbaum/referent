% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select.R
\name{norm_select}
\alias{norm_select}
\title{Lexicographic out-of-sample model ladder}
\usage{
norm_select(specs, data, outcomes, folds = 5, shape_threshold = 0.02, ...)
}
\arguments{
\item{specs}{A named list of \link{norm_spec} objects, simplest first.}

\item{data}{Reference data.}

\item{outcomes}{Outcome selection.}

\item{folds}{Cross-fit folds.}

\item{shape_threshold}{Extra log-score gain required to accept a
covariate-dependent skew/tail model.}

\item{...}{Passed to \code{\link[=norm_crossfit]{norm_crossfit()}}.}
}
\value{
A \code{norm_selection}: the selected spec and name, the selected
model's cross-fit scores, and a \code{comparison} table with the
out-of-fold log score, CRPS, and calibration gate values per model.
}
\description{
Every criterion is computed from out-of-fold cross-fitted scores:
}
\details{
\enumerate{
\item Drop failed or unstable fits.
\item Drop models whose out-of-fold calibration is unacceptable (the
calibration gate below).
\item Rank survivors by out-of-fold mean log score; ties are broken by
out-of-fold CRPS.
\item Keep the candidates within one standard error of the best, where
the standard error is that of the \emph{paired} per-observation
log-density difference against the best model. A candidate whose
CRPS is worse than the best by more than one paired standard error
is dropped as well.
\item Require \code{shape_threshold} extra mean log score before accepting any
spec with covariate-dependent skew or tail.
\item Choose the simplest remaining model (lowest ladder level).
}

The calibration gate passes a model when, for every outcome with
\eqn{n} out-of-fold scores, \eqn{|\bar z| \le 0.10 + 2/\sqrt{n}},
\eqn{0.80 - 2\sqrt{2/n} \le \mathrm{var}(z) \le 1.25 + 2\sqrt{2/n}},
the 95\% coverage lies in \eqn{[0.93, 0.97] \pm 2\sqrt{0.05 \cdot 0.95/n}},
and the observed 5\% two-sided tail rate lies in
\eqn{0.05 \pm (0.015 + 2\sqrt{0.05 \cdot 0.95/n})}. The fixed margins
are practical tolerances; the \eqn{2/\sqrt{n}} terms are two standard
errors under perfect calibration, so small samples are not rejected
for noise alone. The \code{calibrated} column of \code{comparison} records the
gate's verdict per model.
}
