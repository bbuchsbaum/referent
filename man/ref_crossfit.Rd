% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crossfit.R
\name{ref_crossfit}
\alias{ref_crossfit}
\title{Out-of-fold reference scores}
\usage{
ref_crossfit(
  spec,
  data,
  outcomes,
  folds = 5,
  strata = NULL,
  cluster = NULL,
  id = NULL,
  uncertainty = c("conditional", "total"),
  ...
)
}
\arguments{
\item{spec}{A \link{ref_spec}.}

\item{data}{Reference data.}

\item{outcomes}{Outcome selection, as in \code{\link[=ref_fit]{ref_fit()}}.}

\item{folds}{Number of folds (at least 2).}

\item{strata}{Optional stratification column. Rows with a missing
stratum form a stratum of their own.}

\item{cluster}{Optional cluster / subject column. Entire clusters stay
in one fold.}

\item{id}{Optional identifier stored on scores.}

\item{uncertainty}{Passed to \code{\link[=predict.ref_fit]{predict.ref_fit()}} when the held-out
fold is scored, as in \code{\link[=ref_assess]{ref_assess()}}. Use \code{"total"} to match the
predictive that \code{\link[=predict.ref_fit]{predict.ref_fit()}} returns by default, which is
what a map fitted on these scores by \code{\link[=ref_calibrate]{ref_calibrate()}} will be
applied to.}

\item{...}{Passed to \code{\link[=ref_fit]{ref_fit()}}.}
}
\value{
A \code{ref_scores} object with \code{.in_sample = FALSE}, \code{.row}
indexing rows of \code{data}, a per-row \code{crps} column, and a \code{.fold}
column, plus a \code{deployment} fit attribute.
}
\description{
Every reference observation receives a score from a model that did not
include that observation. A final deployment model is then refit on all
reference rows.
}
