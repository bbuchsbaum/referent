% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/support.R
\name{norm_support}
\alias{norm_support}
\title{Reference-support status of target observations}
\usage{
norm_support(fit, newdata)
}
\arguments{
\item{fit}{A \link{norm_fit}.}

\item{newdata}{Target data.}
}
\value{
A tibble with \code{.row}, \code{support}, and the squared Mahalanobis
distance \code{d2} (\code{NA} when there are fewer than two numeric covariates
or the row has a missing covariate).
}
\description{
Checks each row of \code{newdata} against the covariate ranges and the
joint (Mahalanobis) spread of the reference data on the covariates the
model actually uses. Statuses: \code{"in"}, \code{"edge"} (outside the central
96\% of a covariate or beyond the 99\% Mahalanobis radius), \code{"out"}
(outside the observed range or beyond twice that radius), and
\code{"new_group"} (unseen factor level), and \code{"unknown"} (a covariate is
\code{NA}).
}
\examples{
ref <- norm_simulate(80, seed = 1)
spec <- norm_spec(family = norm_gaussian(), location = ~ age + sex)
fit <- norm_fit(spec, data = ref, outcomes = "y")
norm_support(fit, ref[1:3, ])
}
