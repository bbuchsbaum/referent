% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forecast.R
\name{ref_forecast}
\alias{ref_forecast}
\title{History-conditioned forecast}
\usage{
ref_forecast(dynamic, history, times, outcome = NULL)
}
\arguments{
\item{dynamic}{A \link{ref_dynamics} object.}

\item{history}{Subject history data frame. Must contain the time column
used in \code{\link[=ref_dynamics]{ref_dynamics()}} and the outcome.}

\item{times}{Future times.}

\item{outcome}{Outcome name.}
}
\value{
A \code{ref_forecast} with \verb{$dist} (a \code{distribution} vector, one element per time),
\verb{$summary} (median and 90\% interval), \verb{$history_n}, and
\verb{$lag_support} (\code{"in"} or \code{"extrapolated_lag"} per forecast time,
judged from the last history visit against the reference lag range).
}
\description{
Returns a \link{dist_conditioned} predictive distribution at each requested time. The
dynamic process only changes the predictive distribution; scores and
graphics then follow from the existing contract.
}
\details{
The forecast refuses to run when the process for \code{outcome} is not
identified (see \code{\link[=ref_dynamics]{ref_dynamics()}}): a history-conditioned distribution
from an unestimated kernel would be silently wrong. History rows with a
non-finite time or outcome are dropped and the number actually used is
returned as \code{history_n}. The exact marginal uncertainty estimand stored by
\code{\link[=ref_dynamics]{ref_dynamics()}} is reused. Kernel parameters remain plug-in estimates;
their uncertainty is explicitly recorded but not propagated.
}
